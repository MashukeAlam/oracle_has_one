<%= form_with(model: companyinvoice, local: true) do |form| %>
  <% if companyinvoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(companyinvoice.errors.count, "error") %> prohibited this companyinvoice from being saved:</h2>

      <ul>
      <% companyinvoice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :invoicename %>
    <%= form.text_field :invoicename %>
  </div>

  <div class="field">
    <%= form.label :companybill_id %>
    <%= form.text_field :companybill_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
